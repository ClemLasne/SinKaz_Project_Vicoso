A genome admixture analysis was done on F1 RNA seq and 2022 F2BC (Christelle's) samples.

The Plink admixture pipeline gives strange results where F2s appear 100% sinica, but the F1s look relatively close to what is expected (50% sin, 50 kaz). Try making the admixture plot in R using the same data as used for chromopaint.


# Admixture in F2s: alternatively fixed SNPs between sin and kaz, no missing sites

```ruby
chromo<-read.table("~/Documents/SinKaz/ALL_SAMPLES_OCT2023/chromopaint/F2_datasets/SinKaz_Oct2023_filtered_fixedSNP_F2s_nomissing.vcf.proportion", head=F, sep="\t")[,-22]
head(chromo)

colnames(chromo)<-c("gene", "Mal1", "Mal2", "Mal3", "Mal4", "Mal5", "Mal6", "Mal7", "Mal8", "Mal9", "Mal10", "Fem1", "Fem2", "Fem3", "Fem4", "Fem5", "Fem6", "Fem7", "Fem8", "Fem9", "Fem10")
head(chromo)

n_all_sites <- nrow(chromo)
n_all_sites # 2990 sites in dataset


## calculations 

#### F2 Male 1 ####

indiv <- chromo$Mal1
indiv

# number of sites with 100% sinica allele ancestry (sin/sin)
homoz <- nrow(chromo[indiv<0.2, ])
homoz
# number of sites with sin/kaz ancestry 
htzg <- nrow(chromo[indiv>0.8, ])
htzg

# actual total number of sites to use for ancestry analysis
actual_num_sites <- homoz + htzg
actual_num_sites

# number of sites not attributed to either of the 2 categories (homoz vs htzg)
limbo_sites <- n_all_sites - actual_num_sites
limbo_sites

#### diploidy : each site has 2 alleles. Homoz sites have 2 sinica alleles, whereas heterozygous sites only have 1 sinica alleles (and 1 kaza allele)

# number of sinica alleles 
nb_sin_alleles <-  homoz*2 + htzg 

# number of kaza alleles 
nb_kaza_alleles <- htzg

# sanity checks that the numbers add up:
nb_sin_alleles + nb_kaza_alleles
actual_num_sites * 2

### proportion ancestry
sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
sin_ancestry

kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry

# sanity check (should =1)
sin_ancestry + kaza_ancestry

df <- data.frame(row.names = c("sinica ancestry","kaza ancestry"),
Mal1 = c(sin_ancestry, kaza_ancestry))
                 
df




#### F2 Male 2 ####
indiv <- chromo$Mal2

homoz <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz*2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$Mal2 <- c(sin_ancestry, kaza_ancestry)
df


#### F2 Male 3 ####
indiv <- chromo$Mal3

homoz <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz*2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$Mal3 <- c(sin_ancestry, kaza_ancestry)
df


#### F2 Male 4 ####
indiv <- chromo$Mal4

homoz <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz*2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$Mal4 <- c(sin_ancestry, kaza_ancestry)
df

#### F2 Male 5 ####
indiv <- chromo$Mal5

homoz <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz*2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$Mal5 <- c(sin_ancestry, kaza_ancestry)
df


#### F2 Male 6 ####
indiv <- chromo$Mal6

homoz <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz*2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$Mal6 <- c(sin_ancestry, kaza_ancestry)
df

#### F2 Male 7 ####
indiv <- chromo$Mal7

homoz <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz*2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$Mal7 <- c(sin_ancestry, kaza_ancestry)
df

#### F2 Male 8 ####
indiv <- chromo$Mal8

homoz <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz*2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$Mal8 <- c(sin_ancestry, kaza_ancestry)
df

#### F2 Male 9 ####
indiv <- chromo$Mal9

homoz <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz*2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$Mal9 <- c(sin_ancestry, kaza_ancestry)
df

#### F2 Male 10 ####
indiv <- chromo$Mal10

homoz <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz*2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$Mal10 <- c(sin_ancestry, kaza_ancestry)
df


#### F2 Female 1 ####
indiv <- chromo$Fem1

homoz <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz*2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$Fem1 <- c(sin_ancestry, kaza_ancestry)
df

#### F2 Female 2 ####
indiv <- chromo$Fem2

homoz <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz*2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$Fem2 <- c(sin_ancestry, kaza_ancestry)
df


#### F2 Female 3 ####
indiv <- chromo$Fem3

homoz <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz*2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$Fem3 <- c(sin_ancestry, kaza_ancestry)
df

#### F2 Female 4 ####
indiv <- chromo$Fem4

homoz <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz*2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$Fem4 <- c(sin_ancestry, kaza_ancestry)
df

#### F2 Female 5 ####
indiv <- chromo$Fem5

homoz <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz*2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$Fem5 <- c(sin_ancestry, kaza_ancestry)
df

#### F2 Female 6 ####
indiv <- chromo$Fem6

homoz <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz*2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$Fem6 <- c(sin_ancestry, kaza_ancestry)
df

#### F2 Female 7 ####
indiv <- chromo$Fem7

homoz <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz*2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$Fem7 <- c(sin_ancestry, kaza_ancestry)
df

#### F2 Female 8 ####
indiv <- chromo$Fem8

homoz <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz*2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$Fem8 <- c(sin_ancestry, kaza_ancestry)
df

#### F2 Female 9 ####
indiv <- chromo$Fem9

homoz <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz*2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$Fem9 <- c(sin_ancestry, kaza_ancestry)
df

#### F2 Female 10 ####
indiv <- chromo$Fem10

homoz <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz*2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$Fem10 <- c(sin_ancestry, kaza_ancestry)
df



### admixture plot

str(df)

matrix_df <- as.matrix(df)

barplot(matrix_df,
        legend.text=T,
        args.legend = list(x = "bottomright", inset = c(0, 0.2)),
        col=c("blue" , "orange"),
        ylab="Percentage SNP ancestry",
        xlab="",
        las=2,
        main="admixture plot in F2s")

abline(h=75,col="black", lty = 2, lwd=3)

axis(2, seq(0, 100, by=10), labels=FALSE, tck=-.01)
```

![Screenshot 2023-11-02 at 10 47 28](https://github.com/ClemLasne/SinKaz_project/assets/24788785/f21aa181-d47e-4a12-837c-19d08456193c)

# All samples

Dataset used was created in step 02. chromopaint and can be found in this directory on the cluster:
```ruby
/nfs/scistore18/vicosgrp/clasne/SinKaz_hybrid_GE/SNP_calling_Parents_f1s_F2s_Oct2023/chromopaint/all_samples
```

Download the `SinKaz_Oct2023_filtered_fixedSNP_no_missing_sites.vcf.proportion`file and run the following code:

```ruby
chromo<-read.table("~/Documents/SinKaz/ALL_SAMPLES_OCT2023/chromopaint/all_samples/SinKaz_Oct2023_filtered_fixedSNP_no_missing_sites.vcf.proportion", head=F, sep="\t")[,-58]
head(chromo)

colnames(chromo)<-c("gene","SIN_Male_Rep1","SIN_Male_Rep2","SIN_Male1","SIN_Male2",
                    "SIN_Female1","SIN_Female2",
                    "KAZ_Male1","KAZ_Male2",
                    "KAZ_Female_Rep1","KAZ_Female_Rep2","KAZ_Female1","KAZ_Female2",
                    "F2_Male1","F2_Male2","F2_Male3","F2_Male4","F2_Male5","F2_Male6","F2_Male7","F2_Male8","F2_Male9","F2_Male10",
                    "F2_Female1","F2_Female2","F2_Female3","F2_Female4","F2_Female5","F2_Female6","F2_Female7","F2_Female8","F2_Female9","F2_Female10",
                    "F1_Female_KS1","F1_Female_KS2","F1_Female_KS3","F1_Female_KS4","F1_Female_KS5","F1_Female_SK1","F1_Female_SK2","F1_Female_SK3","F1_Female_SK4","F1_Female_SK5","F1_Female_SK6",
                    "F1_Male_KS1","F1_Male_KS2","F1_Male_KS3","F1_Male_KS4","F1_Male_KS5","F1_Male_KS6","F1_Male_SK1","F1_Male_SK2","F1_Male_SK3","F1_Male_SK4","F1_Male_SK5","F1_Male_SK6","F1_Male_SK7")
head(chromo)

n_all_sites <- nrow(chromo)
n_all_sites


## calculations 

#### SIN_Male_Rep1 ####

indiv <- chromo$SIN_Male_Rep1
indiv

# number of sites with 100% sinica allele ancestry (sin/sin)
homoz_sin <- nrow(chromo[indiv<0.2, ])
homoz_sin
# number of sites with sin/kaz ancestry 
homoz_kaz <- nrow(chromo[indiv>0.8, ])
homoz_kaz 

# actual total number of sites to use for ancestry analysis
actual_num_sites <- homoz_kaz + homoz_sin
actual_num_sites

#### diploidy : each site has 2 alleles. Homoz sites have 2 sinica alleles, whereas heterozygous sites only have 1 sinica alleles (and 1 kaza allele)

# number of sinica alleles 
nb_sin_alleles <- homoz_sin * 2 

# number of kaza alleles 
nb_kaza_alleles <- homoz_kaz * 2

# sanity checks that the numbers add up:
nb_sin_alleles + nb_kaza_alleles
actual_num_sites * 2

### proportion ancestry
sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
sin_ancestry

kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry

# sanity check (should =1)
sin_ancestry + kaza_ancestry

df <- data.frame(row.names = c("kaza ancestry", "sinica ancestry"),
                 SIN_Male_Rep1 = c(kaza_ancestry, sin_ancestry))

df


#### SIN_Male_Rep2 ####
indiv <- chromo$SIN_Male_Rep2

homoz_sin <- nrow(chromo[indiv<0.2, ])
homoz_kaz <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz_kaz + homoz_sin

#diploidy
nb_sin_alleles <- homoz_sin * 2 
nb_kaza_alleles <- homoz_kaz * 2

### proportion ancestry
sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$SIN_Male_Rep2 <- c(kaza_ancestry, sin_ancestry)
df



#### SIN_Male1 ####
indiv <- chromo$SIN_Male1

homoz_sin <- nrow(chromo[indiv<0.2, ])
homoz_kaz <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz_kaz + homoz_sin

#diploidy
nb_sin_alleles <- homoz_sin * 2 
nb_kaza_alleles <- homoz_kaz * 2

### proportion ancestry
sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$SIN_Male1 <- c(kaza_ancestry, sin_ancestry)
df


#### SIN_Male2 ####
indiv <- chromo$SIN_Male2

homoz_sin <- nrow(chromo[indiv<0.2, ])
homoz_kaz <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz_kaz + homoz_sin

#diploidy
nb_sin_alleles <- homoz_sin * 2 
nb_kaza_alleles <- homoz_kaz * 2

### proportion ancestry
sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$SIN_Male2 <- c(kaza_ancestry, sin_ancestry)
df


#### SIN_Female1 ####
indiv <- chromo$SIN_Female1

homoz_sin <- nrow(chromo[indiv<0.2, ])
homoz_kaz <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz_kaz + homoz_sin

#diploidy
nb_sin_alleles <- homoz_sin * 2 
nb_kaza_alleles <- homoz_kaz * 2

### proportion ancestry
sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$SIN_Female1 <- c(kaza_ancestry, sin_ancestry)
df


#### SIN_Female2 ####
indiv <- chromo$SIN_Female2

homoz_sin <- nrow(chromo[indiv<0.2, ])
homoz_kaz <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz_kaz + homoz_sin

#diploidy
nb_sin_alleles <- homoz_sin * 2 
nb_kaza_alleles <- homoz_kaz * 2

### proportion ancestry
sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$SIN_Female2 <- c(kaza_ancestry, sin_ancestry)
df


#### KAZ_Male1 ####
indiv <- chromo$KAZ_Male1

homoz_kaz <- nrow(chromo[indiv>0.8, ])
homoz_kaz
homoz_sin <- nrow(chromo[indiv<0.2, ])
homoz_sin
actual_num_sites <- homoz_sin + homoz_kaz

# diploidy
nb_kaza_alleles <-  homoz_kaz * 2
nb_sin_alleles <- homoz_sin * 2

# proportion ancestry
sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
sin_ancestry
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry

df$KAZ_Male1 <- c(kaza_ancestry, sin_ancestry)
df

#### KAZ_Male2 ####
indiv <- chromo$KAZ_Male2

homoz_kaz <- nrow(chromo[indiv>0.8, ])
homoz_kaz
homoz_sin <- nrow(chromo[indiv<0.2, ])
homoz_sin
actual_num_sites <- homoz_sin + homoz_kaz

# diploidy
nb_kaza_alleles <-  homoz_kaz * 2
nb_sin_alleles <- homoz_sin * 2

# proportion ancestry
sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
sin_ancestry
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry

df$KAZ_Male2 <- c(kaza_ancestry, sin_ancestry)
df


#### KAZ_Female_Rep1 ####
indiv <- chromo$KAZ_Female_Rep1

homoz_kaz <- nrow(chromo[indiv>0.8, ])
homoz_kaz
homoz_sin <- nrow(chromo[indiv<0.2, ])
homoz_sin
actual_num_sites <- homoz_sin + homoz_kaz

# diploidy
nb_kaza_alleles <-  homoz_kaz * 2
nb_sin_alleles <- homoz_sin * 2

# proportion ancestry
sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
sin_ancestry
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry

df$KAZ_Female_Rep1 <- c(kaza_ancestry, sin_ancestry)
df

#### KAZ_Female_Rep2 ####
indiv <- chromo$KAZ_Female_Rep2

homoz_kaz <- nrow(chromo[indiv>0.8, ])
homoz_kaz
homoz_sin <- nrow(chromo[indiv<0.2, ])
homoz_sin
actual_num_sites <- homoz_sin + homoz_kaz

# diploidy
nb_kaza_alleles <-  homoz_kaz * 2
nb_sin_alleles <- homoz_sin * 2

# proportion ancestry
sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
sin_ancestry
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry

df$KAZ_Female_Rep2 <- c(kaza_ancestry, sin_ancestry)
df

#### KAZ_Female1 ####
indiv <- chromo$KAZ_Female1

homoz_kaz <- nrow(chromo[indiv>0.8, ])
homoz_kaz
homoz_sin <- nrow(chromo[indiv<0.2, ])
homoz_sin
actual_num_sites <- homoz_sin + homoz_kaz

# diploidy
nb_kaza_alleles <-  homoz_kaz * 2
nb_sin_alleles <- homoz_sin * 2

# proportion ancestry
sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
sin_ancestry
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry

df$KAZ_Female1 <- c(kaza_ancestry, sin_ancestry)
df

#### KAZ_Female2 ####
indiv <- chromo$KAZ_Female2 

homoz_kaz <- nrow(chromo[indiv>0.8, ])
homoz_kaz
homoz_sin <- nrow(chromo[indiv<0.2, ])
homoz_sin
actual_num_sites <- homoz_sin + homoz_kaz

# diploidy
nb_kaza_alleles <-  homoz_kaz * 2
nb_sin_alleles <- homoz_sin * 2

# proportion ancestry
sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
sin_ancestry
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry

df$KAZ_Female2 <- c(kaza_ancestry, sin_ancestry)
df


#### F2_Male1 ####
indiv <- chromo$F2_Male1

homoz_sin <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz_sin + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz_sin * 2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$F2_Male1 <- c(kaza_ancestry, sin_ancestry)
df

#### F2_Male2 ####
indiv <- chromo$F2_Male2

homoz_sin <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz_sin + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz_sin * 2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$F2_Male2 <- c(kaza_ancestry, sin_ancestry)
df

#### F2_Male3 ####
indiv <- chromo$F2_Male3

homoz_sin <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz_sin + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz_sin * 2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$F2_Male3 <- c(kaza_ancestry, sin_ancestry)
df

#### F2_Male4 ####
indiv <- chromo$F2_Male4

homoz_sin <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz_sin + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz_sin * 2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$F2_Male4 <- c(kaza_ancestry, sin_ancestry)
df

#### F2_Male5 ####
indiv <- chromo$F2_Male5

homoz_sin <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz_sin + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz_sin * 2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$F2_Male5 <- c(kaza_ancestry, sin_ancestry)
df

#### F2_Male6 ####
indiv <- chromo$F2_Male6

homoz_sin <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz_sin + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz_sin * 2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$F2_Male6 <- c(kaza_ancestry, sin_ancestry)
df

#### F2_Male7 ####
indiv <- chromo$F2_Male7

homoz_sin <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz_sin + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz_sin * 2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$F2_Male7 <- c(kaza_ancestry, sin_ancestry)
df

#### F2_Male8 ####
indiv <- chromo$F2_Male8

homoz_sin <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz_sin + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz_sin * 2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$F2_Male8 <- c(kaza_ancestry, sin_ancestry)
df

#### F2_Male9 ####
indiv <- chromo$F2_Male9

homoz_sin <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz_sin + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz_sin * 2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$F2_Male9 <-c(kaza_ancestry, sin_ancestry)
df

#### F2_Male10 ####
indiv <- chromo$F2_Male10

homoz_sin <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz_sin + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz_sin * 2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$F2_Male10 <- c(kaza_ancestry, sin_ancestry)
df


#### F2_Female1 ####
indiv <- chromo$F2_Female1

homoz_sin <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz_sin + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz_sin * 2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$F2_Female1 <- c(kaza_ancestry, sin_ancestry)
df

#### F2_Female2 ####
indiv <- chromo$F2_Female2

homoz_sin <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz_sin + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz_sin * 2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$F2_Female2 <- c(kaza_ancestry, sin_ancestry)
df

#### F2_Female3 ####
indiv <- chromo$F2_Female3

homoz_sin <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz_sin + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz_sin * 2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$F2_Female3 <- c(kaza_ancestry, sin_ancestry)
df

#### F2_Female4 ####
indiv <- chromo$F2_Female4

homoz_sin <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz_sin + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz_sin * 2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$F2_Female4 <- c(kaza_ancestry, sin_ancestry)
df

#### F2_Female5 ####
indiv <- chromo$F2_Female5

homoz_sin <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz_sin + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz_sin * 2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$F2_Female5 <- c(kaza_ancestry, sin_ancestry)
df

#### F2_Female6 ####
indiv <- chromo$F2_Female6

homoz_sin <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz_sin + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz_sin * 2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$F2_Female6 <- c(kaza_ancestry, sin_ancestry)
df

#### F2_Female7 ####
indiv <- chromo$F2_Female7

homoz_sin <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz_sin + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz_sin * 2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$F2_Female7 <- c(kaza_ancestry, sin_ancestry)
df

#### F2_Female8 ####
indiv <- chromo$F2_Female8

homoz_sin <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz_sin + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz_sin * 2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$F2_Female8 <- c(kaza_ancestry, sin_ancestry)
df

#### F2_Female9 ####
indiv <- chromo$F2_Female9

homoz_sin <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz_sin + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz_sin * 2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$F2_Female9 <- c(kaza_ancestry, sin_ancestry)
df

#### F2_Female10 ####
indiv <- chromo$F2_Female10

homoz_sin <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz_sin + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz_sin * 2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$F2_Female10<- c(kaza_ancestry, sin_ancestry)
df



#### F1_Female_KS1 ####
indiv <- chromo$F1_Female_KS1

homoz_sin <- nrow(chromo[indiv<0.2, ])
homoz_sin

htzg <- nrow(chromo[indiv>0.8, ])
htzg

actual_num_sites <- homoz_sin + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz_sin * 2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$F1_Female_KS1 <- c(kaza_ancestry, sin_ancestry)
df

#### F1_Female_KS2 ####
indiv <- chromo$F1_Female_KS2

homoz_sin <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz_sin + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz_sin * 2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$F1_Female_KS2 <- c(kaza_ancestry, sin_ancestry)
df


#### F1_Female_KS3 ####
indiv <- chromo$F1_Female_KS3

homoz_sin <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz_sin + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz_sin * 2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$F1_Female_KS3 <- c(kaza_ancestry, sin_ancestry)
df

#### F1_Female_KS4 ####
indiv <- chromo$F1_Female_KS4

homoz_sin <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz_sin + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz_sin * 2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$F1_Female_KS4 <- c(kaza_ancestry, sin_ancestry)
df


#### F1_Female_KS5 ####
indiv <- chromo$F1_Female_KS5

homoz_sin <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz_sin + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz_sin * 2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$F1_Female_KS5 <- c(kaza_ancestry, sin_ancestry)
df

#### F1_Female_SK1 ####
indiv <- chromo$F1_Female_SK1

homoz_sin <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz_sin + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz_sin * 2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$F1_Female_SK1 <- c(kaza_ancestry, sin_ancestry)
df

#### F1_Female_SK2 ####
indiv <- chromo$F1_Female_SK2

homoz_sin <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz_sin + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz_sin * 2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$F1_Female_SK2 <- c(kaza_ancestry, sin_ancestry)
df


#### F1_Female_SK3 ####
indiv <- chromo$F1_Female_SK3

homoz_sin <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz_sin + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz_sin * 2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$F1_Female_SK3 <- c(kaza_ancestry, sin_ancestry)
df

#### F1_Female_SK4 ####
indiv <- chromo$F1_Female_SK4

homoz_sin <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz_sin + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz_sin * 2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$F1_Female_SK4 <- c(kaza_ancestry, sin_ancestry)
df

#### F1_Female_SK5 ####
indiv <- chromo$F1_Female_SK5

homoz_sin <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz_sin + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz_sin * 2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$F1_Female_SK5 <- c(kaza_ancestry, sin_ancestry)
df

#### F1_Female_SK6 ####
indiv <- chromo$F1_Female_SK6

homoz_sin <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz_sin + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz_sin * 2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$F1_Female_SK6 <- c(kaza_ancestry, sin_ancestry)
df

#### F1_Male_KS1 ####
indiv <- chromo$F1_Male_KS1

homoz_sin <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz_sin + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz_sin * 2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$F1_Male_KS1 <- c(kaza_ancestry, sin_ancestry)
df

#### F1_Male_KS2 ###
indiv <- chromo$F1_Male_KS2

homoz_sin <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz_sin + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz_sin * 2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$F1_Male_KS2 <- c(kaza_ancestry, sin_ancestry)
df

#### F1_Male_KS3 ###
indiv <- chromo$F1_Male_KS3

homoz_sin <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz_sin + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz_sin * 2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$F1_Male_KS3 <- c(kaza_ancestry, sin_ancestry)
df


#### F1_Male_KS4 ###
indiv <- chromo$F1_Male_KS4

homoz_sin <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz_sin + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz_sin * 2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$F1_Male_KS4 <- c(kaza_ancestry, sin_ancestry)
df

#### F1_Male_KS5 ###
indiv <- chromo$F1_Male_KS5

homoz_sin <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz_sin + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz_sin * 2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$F1_Male_KS5 <- c(kaza_ancestry, sin_ancestry)
df

#### F1_Male_KS6 ###
indiv <- chromo$F1_Male_KS6

homoz_sin <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz_sin + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz_sin * 2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$F1_Male_KS6 <- c(kaza_ancestry, sin_ancestry)
df

#### F1_Male_SK1 ###
indiv <- chromo$F1_Male_SK1

homoz_sin <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz_sin + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz_sin * 2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$F1_Male_SK1 <- c(kaza_ancestry, sin_ancestry)
df

#### F1_Male_SK2 ###
indiv <- chromo$F1_Male_SK2

homoz_sin <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz_sin + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz_sin * 2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$F1_Male_SK2 <- c(kaza_ancestry, sin_ancestry)
df

#### F1_Male_SK3 ###
indiv <- chromo$F1_Male_SK3

homoz_sin <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz_sin + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz_sin * 2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$F1_Male_SK3 <- c(kaza_ancestry, sin_ancestry)
df

#### F1_Male_SK4 ###
indiv <- chromo$F1_Male_SK4

homoz_sin <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz_sin + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz_sin * 2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$F1_Male_SK4 <- c(kaza_ancestry, sin_ancestry)
df

#### F1_Male_SK5 ###
indiv <- chromo$F1_Male_SK5

homoz_sin <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz_sin + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz_sin * 2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$F1_Male_SK5 <- c(kaza_ancestry, sin_ancestry)
df

#### F1_Male_SK6 ###
indiv <- chromo$F1_Male_SK6

homoz_sin <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz_sin + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz_sin * 2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$F1_Male_SK6 <- c(kaza_ancestry, sin_ancestry)
df

#### F1_Male_SK7 ###
indiv <- chromo$F1_Male_SK7

homoz_sin <- nrow(chromo[indiv<0.2, ])
htzg <- nrow(chromo[indiv>0.8, ])
actual_num_sites <- homoz_sin + htzg
limbo_sites <- n_all_sites - actual_num_sites

nb_sin_alleles <-  homoz_sin * 2 + htzg 
nb_kaza_alleles <- htzg

sin_ancestry <- nb_sin_alleles / (nb_sin_alleles + nb_kaza_alleles) *100
kaza_ancestry <- nb_kaza_alleles / (nb_sin_alleles + nb_kaza_alleles) *100

df$F1_Male_SK7 <- c(kaza_ancestry, sin_ancestry)
df




### Admixture plot

### admixture plot

str(df)

matrix_df <- as.matrix(df)

barplot(matrix_df,
        legend.text=T,
        args.legend = list(x = "bottomright", inset = c(0, 0.7)),
        col=c( "orange", "blue"),
        ylab="Percentage SNP ancestry",
        xlab="",
        las=2,
        main="R admixture plot in all samples - fixed SNPs, no missing sites (±2k sites) .proportion file")

abline(h=c(25, 50),col="black", lty = 2, lwd=3)

axis(2, seq(0, 100, by=10), labels=FALSE, tck=-.01)
```

<img width="1183" alt="Screenshot 2023-11-02 at 16 56 39" src="https://github.com/ClemLasne/SinKaz_project/assets/24788785/2e10ba06-b2a8-4e09-822c-e2a73ba5aaaa">
